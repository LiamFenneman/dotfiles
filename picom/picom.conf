blur: {
    method = "dual_kawase";
    strength = 10;
    background = false;
    background-frame = false;
    background-fixed = false;
}

opacity-rule = [
    "90:class_g = 'kitty'",
    "90:class_g = 'Org.gnome.Nautilus'",
];

unredir-if-possible = true;

backend = "glx"
